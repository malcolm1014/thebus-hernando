{
  "name": "thebus-hernando-backend",
  "version": "1.0.0",
  "description": "TriBus ETL server: downloads GTFS feeds from multiple Florida transit agencies (Hernando, Pasco, HART/Tampa -- Citrus pending, see README), merges them into transit_data.json for the offline TriBus mobile client. Serves only /api/version and /api/download -- it does NOT answer rider queries.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "etl": "node src/etl.js",
    "dev": "node --watch server.js",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "adm-zip": "^0.5.10",
    "csv-parse": "^5.5.6",
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3"
  }
}
